APP_NAME="iCore Agent Platform"
APP_VERSION="0.1.0"
DEBUG=true

API_HOST=0.0.0.0
API_PORT=11001

# Used only by production compose; kept here so domain env keys stay aligned.
ICORE_INFRA_ACCESS_NETWORK_NAME=project-icore-agent-infra-access
