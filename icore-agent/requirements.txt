# iCore Agent Platform — Python dependencies
# Generated from pyproject.toml + conda env dp (Python 3.13)
# Install: pip install -r requirements.txt

# ── Strands Agents SDK ────────────────────────
strands-agents==1.35.0
strands-agents-tools==0.4.1

# ── Web framework ─────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.38.0
sse-starlette==2.3.6

# ── Pydantic ──────────────────────────────────
pydantic==2.12.5
pydantic-settings==2.13.1
email-validator==2.3.0

# ── LLM routing ───────────────────────────────
litellm==1.83.8

# ── HTTP client ───────────────────────────────
httpx==0.28.1

# ── Env & logging ─────────────────────────────
python-dotenv==1.0.1
structlog==25.5.0
PyJWT==2.12.1

# ── Redis (conversation memory) ───────────────
redis==5.0.1
hiredis==3.3.1

# ── Structured data uploads (Data agent) ──────
pandas==2.2.3
openpyxl==3.1.5

# ── Email service (Resend) ────────────────────
resend==2.30.0
