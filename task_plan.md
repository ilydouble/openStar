# iCore 智能工作台岗位任务化改造计划

## 目标
将现有工作台的通用能力按钮，调整为面向跨境小团队和一人公司的 AI 运营岗位入口。前端先完成产品语义、任务卡和响应提示结构改造，后端保留现有聊天接口，后续可替换为真实任务执行流程。

## 阶段

1. [complete] 识别现有工作台入口、场景模板和后端 agent hint 连接方式。
2. [complete] 设计低风险的信息架构：岗位卡、任务按钮、输入/输出/审核提示。
3. [complete] 修改前端文案和界面结构，使按钮表达为岗位职责和任务结果。
4. [complete] 更新场景 prompt，使 AI 响应更像任务执行结果。
5. [complete] 运行前端测试和构建验证。
6. [complete] 按仓库要求提交 scoped git commit。
7. [complete] 根据用户反馈，将任务卡从展示型改成轻量选择型。

## 关键决策

- 暂不重做后端接口，继续复用 `chatStream`、`templateId` 和 `agent_hint`。
- 首页先展示少量 AI 岗位，每个岗位下放 2 个高频任务启动器。
- 选中岗位后展示建议输入、默认产出、执行阶段和快速任务。
- 后端未来可以根据 `templateId` 或任务文案映射到真实作业编排。
- 本次未做浏览器 UI 检查：Vite dev server 在当前沙箱下无法监听 `127.0.0.1:5173`，用户表示会自行重启查看。
- 用户反馈任务卡看起来复杂，后续改为首页 4 个核心助理、每个 2 个任务按钮，详细执行说明延后展示。
